# ──────────────────────────────────────────
# Requirements for the Egyptian Medical AI API
# ──────────────────────────────────────────

# Web framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client (used to call Gemini)
requests>=2.31.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0

# Embeddings model (weights loaded once at startup; no bulk encode)
sentence-transformers>=2.7.0

# Fuzzy string matching — replaces FAISS for drug lookup
rapidfuzz>=3.6.0

# Pydantic (FastAPI dependency)
pydantic>=2.7.0

# NOTE: faiss-cpu has been intentionally removed.
# Drug retrieval now uses rapidfuzz directly on the ingredient text column,
# avoiding the 26 000-row bulk encode that caused OOM crashes on Railway.
